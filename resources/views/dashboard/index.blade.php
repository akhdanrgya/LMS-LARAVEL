{{-- @if (auth()->user()->role == 'mentor')
    <p>Welcome Mentor!</p>
@else
    <p>Welcome Student!</p>
@endif

<form action="{{ route('logout') }}" method="POST">
    @csrf
    <button type="submit">Logout</button>
</form> --}}
